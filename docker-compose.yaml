services:
  nginx:
    image: ivandreyv/nginx-alpine-aarch64:3.20.3
    read_only: true
    container_name: nginx
    ports:
      - 80:80
      - 443:443
    tmpfs:
      - /run/nginx/
      - /var/run/
    volumes:
      - ./logs:/var/log/nginx

